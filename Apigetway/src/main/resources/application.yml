server:
  port: 8090
spring:
  application:
    name: apigetway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
zuul:
  routes:
    user:
      path: /apiuser/**
      serviceId: user